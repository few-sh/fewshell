name: agent_core
description: Shared agent loop for Decamp client and server
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.5.0

dependencies:
  dartssh2: ^2.10.1
  http: ^1.2.0
  llm_dart:
    path: ../llm_dart

dev_dependencies:
  lints: ^5.0.0
  test: ^1.25.0
