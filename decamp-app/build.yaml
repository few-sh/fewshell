# Build configuration for code generation
targets:
  $default:
    builders:
      # Freezed configuration
      freezed:
        generate_for:
          - lib/models/*.dart
        options:
          # Generate copyWith methods
          copyWith: true
          # Generate equality methods
          equal: true
          # Generate toString methods
          to_string: true
          # Generate fromJson/toJson
          json: true
          
      # Drift configuration
      drift_dev:
        options:
          # Store datetime as unix timestamps
          store_date_time_values_as_text: false
          # Use data class name for companions (instead of deprecated option)
          use_data_class_name_for_companions: true
          
      # JSON Serializable configuration  
      json_serializable:
        generate_for:
          - lib/models/*.dart
        options:
          # Generate checked fromJson methods
          checked: true
          # Generate toJson methods
          any_map: false
          # Fail on null for non-nullable fields
          disallow_unrecognized_keys: false
